{
  "name": "night-shift-generator",
  "version": "1.0.0",
  "description": "Night Shift by Does News — nightly script generator (Cloudflare Worker)",
  "private": true,
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240917.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.78.0"
  }
}
